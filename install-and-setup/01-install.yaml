# Install Istio

- Follow the link
https://istio.io/latest/docs/setup/getting-started/

-- demo is a profile
``` 
istioctl install --set profile=demo -y

- Which comes with default configuration values that are useful for your demo 
- IF you are going with production thenyou will get most strict values 
```
## deploy the application
## Follow the link to deploy the application

-- We will get all the pods running  like 6 PODS, Here every POD is running as 2 one is application
-- Second one is SIDE-CAR CONTAINER.
